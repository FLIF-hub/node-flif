/* eslint-disable no-console */
/* eslint-disable no-unused-vars */

var fs = require('fs');
var path = require('path');

function encodeFLIF (params) {
    console.log(params);
}

function decodeFLIF (params) {
    console.log(params);
}
