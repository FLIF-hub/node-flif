function testDecode () {
    // var decode = require('./decode.js');
    var testData = [
        { 'item': '', 'expectation': '' }
    ];

    return ['decode', testData.length];
}

module.exports = testDecode;
