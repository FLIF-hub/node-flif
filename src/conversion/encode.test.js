function testEncode () {
    // var encode = require('./encode.js');
    var testData = [
        { 'item': '', 'expectation': '' }
    ];

    return ['encode', testData.length];
}

module.exports = testEncode;
