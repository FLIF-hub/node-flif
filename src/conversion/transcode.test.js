function testTranscode () {
    // var transcode = require('./transcode.js');
    var testData = [
        { 'item': '', 'expectation': '' }
    ];

    return ['transcode', testData.length];
}

module.exports = testTranscode;
