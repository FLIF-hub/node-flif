function testRunCommand () {
    // var runCommand = require('./runcommand.js');
    var testData = [
        { 'item': '', 'expectation': '' }
    ];

    return ['runCommand', testData.length];
}

module.exports = testRunCommand;
