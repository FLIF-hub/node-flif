function test () {
    var testName = 'warnUser';

    // The function is used to console log out errors to the user.
    // Nothing is returned, so nothing can be tested.

    return [testName, 0];
}

module.exports = test;
