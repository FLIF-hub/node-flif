function testBreakpoints () {
    //var breakpoints = require('./breakpoints.js');
    var testData = [
        { 'item': '', 'expectation': '' }
    ];

    return ['breakpoints', testData.length];
}

module.exports = testBreakpoints;
